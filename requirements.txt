# Runtime dependencies for Ubuntu 22.04 (Python 3.10/3.11).
#
# Install PyTorch separately first when using an NVIDIA CUDA wheel.  This
# file deliberately does not pin torch so pip cannot replace a CUDA build
# with a CPU build.  Ultralytics will install a compatible CPU build when
# running the CPU-only setup documented in docs/UBUNTU22_DEPLOY.md.

# GUI and rendering
PySide6>=6.5.0
pyqtgraph>=0.13.0
PyOpenGL>=3.1.0
PyOpenGL_accelerate>=3.1.0

# Computer vision and numerical processing
opencv-python>=4.8.0,<5
numpy>=1.24.0,<2
scipy>=1.10.0
Pillow>=9.0.0
PyYAML>=6.0

# Camera/audio and visual detectors
sounddevice>=0.4.6
mediapipe==0.10.9
ultralytics>=8.0.0

# Default ASR engine (SenseVoice-Small, downloaded on first use)
sherpa-onnx>=1.10.0
